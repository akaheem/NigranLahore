# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment — server/README.md and server/.env.example both instruct
# `cp .env.example .env`, so the file this workflow creates must never be
# committed. `.env.*` would swallow the template, hence the negation.
.env
.env.*
!.env.example

# Test artifacts
test-results/
playwright-report/
coverage/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
